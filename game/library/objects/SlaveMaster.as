package game.library.objects {        import flash.events.TimerEvent;	import flash.display.MovieClip;    import flash.geom.Point;    import flash.utils.Timer;        import game.library.objects.Slave;	import game.library.objects.Whip;        public class SlaveMaster extends flash.display.MovieClip {                private var whip:MovieClip = new Whip();                public function SlaveMaster()        {            whip.name = "whip";            this.addChild(whip);        }                public function doWhip(slave:Slave,                               stageX:Number,                               stageY:Number):void {                    }    }}